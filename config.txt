## CONFIGURAÇÃO DO NGINX  COMANDO 'sudo nano /etc/nginx/nginx.conf
## SALVAR 1 - sudo nginx -t   2- sudo systemctl restart nginx 2- sudo systemctl reload nginx

## ADCIONAR AS PORTAS COMANDO ' sudo ufw allow 8446/tcp


user www-data;
worker_processes auto;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 768;
}

http {
    sendfile on;
    tcp_nopush on;
    types_hash_max_size 2048;
    server_tokens off;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES128-GC>';
    ssl_dhparam /etc/ssl/certs/dhparam.pem;

    access_log /var/log/nginx/access.log;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;

    gzip on;
    gzip_types text/plain application/xml application/json text/css application/javascript;
    gzip_min_length 1000;
  # Configuração para o bot Ceece Gril
    server {
        listen 8443 ssl;
        listen [::]:8443 ssl;

        server_name chatceecegril.antoniooliveira.shop;

        ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
        ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

        root /var/www/html;
        index index.html index.htm;

        location / {
            proxy_pass http://127.0.0.1:3002;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    } # Configuração para o bot Loja Sterel
    server {
        listen 8444 ssl;
        listen [::]:8444 ssl;

        server_name chatlojasterel.antoniooliveira.shop;

        ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
        ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

        root /var/www/html/bot2;
        index index.html index.htm;

        location / {
            proxy_pass http://127.0.0.1:3003;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    }

  # Configuração para o bot Loja Marciowh
    server {
        listen 8445 ssl;
        listen [::]:8445 ssl;

        server_name chatmarciowh.antoniooliveira.shop;

        ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
        ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

        root /var/www/html/bot3;
        index index.html index.htm;

        location / {
            proxy_pass http://127.0.0.1:3004;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

}


# Configuração para o bot Consultorio AR
server {
    listen 8446 ssl;
    listen [::]:8446 ssl;

    server_name chatconsultorioar.antoniooliveira.shop;

    ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
    ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;

    root /var/www/html/bot4;  # Ajuste para o diretório correto
    index index.html index.htm;

    location / {
        proxy_pass http://127.0.0.1:3005;  # Ajuste a porta do serviço que irá rodar
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

}
