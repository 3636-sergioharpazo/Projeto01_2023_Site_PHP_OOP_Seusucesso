app = "projeto01-2023-site-php-oop-seusucesso"

app = "nome-do-seu-app"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"  # Use o Dockerfile que você acabou de criar

[[services]]
  internal_port = 3000  # A porta exposta pela aplicação no Docker
  processes = ["app"]

[[services.ports]]
  handlers = ["http"]
  port = 80
  force_https = true

[[services.ports]]
  handlers = ["tls", "http"]
  port = 443
